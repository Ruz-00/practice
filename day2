const item = {
  name: "Pizza",          // create item object
  price: 500
};

console.log(item.name);          // read
console.log(item.price);

item.name = "Pizza";         // write
item.price = 500;
  
